using ColossalFramework.UI;
using ThemeMixer.UI;
using UnityEngine;

public class ThemeMixerTool : ToolBase
{
    UIToggle toggle = new UIToggle();

    protected override void OnToolUpdate()
    {
        base.OnToolUpdate();

        // Your logic for when the tool is active
    }

    protected override void OnToolGUI(Event e)
    {
        base.OnToolGUI(e);

        // Your GUI logic for the tool
    }

    // Define the method that will be called within the lambda expression
    
    
}
